{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node Server.js"
  },
  "dependencies": {
    "express": "^4.16.2",
    "nodemon": "^1.12.4",
    "normalize.css": "^8.0.0"
  }
}
